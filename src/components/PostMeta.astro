---
import CategoryBadge from './CategoryBadge.astro';
import DifficultyBadge from './DifficultyBadge.astro';
import { formatDate, formatReadingTime } from '../lib/utils';
import type { CategoryKey } from '../lib/constants';

interface Props {
  pubDate: Date;
  minutesRead?: string;
  category: CategoryKey;
  difficulty?: 'beginner' | 'intermediate' | 'advanced';
  showCategory?: boolean;
  linked?: boolean;
}

const { pubDate, minutesRead, category, difficulty, showCategory = true, linked = true } = Astro.props;
---

<div class="flex flex-wrap items-center gap-2 text-sm text-zinc-500 dark:text-zinc-400">
  {showCategory && <CategoryBadge category={category} linked={linked} />}
  <time datetime={pubDate.toISOString()}>
    {formatDate(pubDate)}
  </time>
  {minutesRead && (
    <>
      <span>&middot;</span>
      <span>{formatReadingTime(minutesRead)}</span>
    </>
  )}
  {difficulty && (
    <>
      <span>&middot;</span>
      <DifficultyBadge difficulty={difficulty} />
    </>
  )}
</div>
